import Start from '../components/pages/Start/Start';
import Bar from '../components/pages/Bar/Bar';

export const ROUTES = [
         {
           name: "Start",
           path: "/start",
           component: Start
         },
         {
           name: "Bar",
           path: "/bar",
           component: Bar
         }
       ];